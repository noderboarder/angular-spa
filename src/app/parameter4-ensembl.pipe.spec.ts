import { Parameter4EnsemblPipe } from './parameter4-ensembl.pipe';

describe('Parameter4EnsemblPipe', () => {
  it('create an instance', () => {
    const pipe = new Parameter4EnsemblPipe();
    expect(pipe).toBeTruthy();
  });
});
